module.exports = function ()
{
    return async context =>
    {

        // console.log("I am in forget password")
        // console.log(context.data.email)
        // console.log(context.data.password)
        // const userService = context.app.service("/forgetpassword");
        // const user = await userService.find({ query: { email: context.data.email } })
        // console.log(user.data)
        // if (!user) throw new Error("User not found")
        // delete context.data.email

        // throw new Error("User not found GGGGGGGGGGG")
    }
}